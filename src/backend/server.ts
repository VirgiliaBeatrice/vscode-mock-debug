import { BackendService, IBackendService } from "./service";
// import * as Path from "path";

export class GDBServer extends BackendService implements IBackendService
{
	// constructor(private root: string, private paths?: string[]) {
	// 	super("Subprocess for GDB Debugger Instance");

	// 	if (this.root === undefined) {
	// 		this.root = "C:\\msys32";
	// 	}

	// 	if (this.paths === undefined) {
	// 		this.paths = [
	// 			"mingw32\\bin",
	// 			"usr\\local\\bin",
	// 			"usr\\bin"
	// 		];
	// 	}

	// }

}